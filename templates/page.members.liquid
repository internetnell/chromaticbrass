<style>
    .member-list {
        display: flex;
    }

    .outest {

        padding-left: 60px;
        padding-right: 60px;
    }

    .members-list-title {
        color: var(--Neutral-contrast, #000);
        leading-trim: both;
        text-edge: cap;
        font-family: var(--font-body, "Helvetica Now Display");
        font-size: var(--size-h2, 40px);
        font-style: normal;
        font-weight: 400;
        line-height: 120%;
        letter-spacing: -0.2px;
        margin-bottom: 50px;
    }
</style>

<main>
    {% section 'page-title' %}
    {% section 'members-cards' %}
    <div class="outest">
        <div class="members-list-title">
            Leadership
        </div>
        <div class="member-list">
            {% render 'member-snippet', name: "Willy", title: "Silly" %}
            {% render 'member-snippet', name: "Idk", title: "Playing on their horn" %}
            {% render 'member-snippet', name: "Three", title: "Im tapped outta titles" %}
        </div>
    </div>
</main>